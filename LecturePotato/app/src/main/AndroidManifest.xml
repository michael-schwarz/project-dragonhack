<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.m_schwarz.lecturepotato">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo1"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:label="When does the lecture end?">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ChooseUni"
            android:label="Choose your poison!" />
        <activity
            android:name=".ChooseUsername"
            android:label="Enter battle as" />
        <activity
            android:name=".LeaderboardActivity"
            android:label="Am I awesome yet?"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>